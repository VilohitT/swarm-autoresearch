[project]
name = "swarm-autoresearch"
version = "0.1.0"
description = "Population-based evolutionary algorithm wrapping karpathy/autoresearch"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.40.0",
    "pyyaml>=6.0",
    "matplotlib>=3.7",
]

[project.scripts]
swarm = "orchestrator:run_swarm"
